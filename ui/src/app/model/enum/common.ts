export enum toastType {
  succes,
  error,
  info,
  warning
}

export enum ErrorPageEnum {
  Unauthorized,
  Forbidden,
  ServerError
}

export enum Gender {
  Male = 0,
  Female = 1,
}

export enum RankOfDoctor {
  thacSi,
  tienSi,
  PGSTS,
  GSTS,
  none
}

export enum PositionOfDoctor {
  dean,
  viceDean,
  none
}